# Email: ambhutan@gmail.com | hello@aakash-pradhan.com
# Website: ambhutan.com | aakash-pradhan.com
# Phone: +975 - 1750 - 5267

apiVersion: apps/v1
kind: Deployment
metadata: { name: result-service }
spec:
  replicas: 2
  selector: { matchLabels: { app: result-service } }
  template:
    metadata: { labels: { app: result-service } }
    spec:
      serviceAccountName: result-service
      securityContext: { runAsNonRoot: true, seccompProfile: { type: RuntimeDefault } }
      containers:
        - name: result-service
          image: registry.example.gov.bt/dzongjuk/result-service:replace
          ports: [{ containerPort: 8004 }]
          env: [{ name: DATABASE_NAME, value: dzongjuk_result }]
          envFrom: [{ configMapRef: { name: dzongjuk-config } }, { secretRef: { name: dzongjuk-secrets } }]
          readinessProbe: { httpGet: { path: /health/ready, port: 8004 }, periodSeconds: 10 }
          livenessProbe: { httpGet: { path: /health/live, port: 8004 }, periodSeconds: 15 }
          resources: { requests: { cpu: 200m, memory: 256Mi }, limits: { cpu: 1500m, memory: 768Mi } }
          securityContext: { allowPrivilegeEscalation: false, readOnlyRootFilesystem: true, capabilities: { drop: [ALL] } }
---
apiVersion: v1
kind: Service
metadata: { name: result-service }
spec: { selector: { app: result-service }, ports: [{ port: 8004, targetPort: 8004 }] }
---
apiVersion: v1
kind: ServiceAccount
metadata: { name: result-service }
automountServiceAccountToken: false
---
apiVersion: policy/v1
kind: PodDisruptionBudget
metadata: { name: result-service }
spec: { minAvailable: 1, selector: { matchLabels: { app: result-service } } }
---
apiVersion: autoscaling/v2
kind: HorizontalPodAutoscaler
metadata: { name: result-service }
spec:
  scaleTargetRef: { apiVersion: apps/v1, kind: Deployment, name: result-service }
  minReplicas: 2
  maxReplicas: 10
  metrics: [{ type: Resource, resource: { name: cpu, target: { type: Utilization, averageUtilization: 60 } } }]
